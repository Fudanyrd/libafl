result

sqlite
fuzzer_ossfuzz*

xml2
fuzzer_xml2*

openssl
fuzzer_openssl*
*.bc

bloaty
fuzzer_bloaty*

curl
curl_fuzzer
curl-deps
curl_install
fuzzer_curl*
zlib

freetype2

harfbuzz
fuzzer_hbshape*

json
fuzzer_json*

re2
fuzzer_re2*

zlib-fuzz 
fuzzer_libz*

fuzzer_stats.csv
